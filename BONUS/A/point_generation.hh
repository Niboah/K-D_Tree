#ifndef POINT_GENERATION
#define POINT_GENERATION

#include <vector>
#include <random>
#include <limits>
using namespace std;

int get_random_int(int min, int max);

vector<double> generate_point(int k);

#endif